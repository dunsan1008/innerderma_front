# 다국어 텍스트 줄바꿈 규칙

서비스는 한국어, 영어, 중국어, 일본어를 지원하므로 언어별 특성에 맞는 자연스러운 줄바꿈을 적용한다.

## 한국어

- 한국어 문장형 텍스트는 기본적으로 어절(단어) 단위로 자연스럽게 줄바꿈한다.
- 단어 중간에서 문자가 분리되어 줄바꿈되지 않도록 한다.
- 한국어 텍스트에는 기본적으로 다음 규칙을 적용한다.

```css
word-break: keep-all;
overflow-wrap: break-word;
```

## 영어

- 영어 문장형 텍스트는 기본적으로 단어 단위로 자연스    럽게 줄바꿈한다.
- 단어 중간에서 불필요하게 줄바꿈하지 않는다.
- 영어에는 한국어의 `word-break: keep-all`을 적용하지 않는다.
- 기본적으로 다음 규칙을 사용한다.

```css
word-break: normal;
overflow-wrap: break-word;
```

## 중국어

- 중국어는 띄어쓰기로 단어를 구분하지 않는 언어 특성을 고려한다.
- 문장 길이에 따라 자연스럽게 문자 단위 줄바꿈이 가능하도록 한다.
- 한국어와 동일한 `word-break: keep-all`을 적용하지 않는다.
- 기본적으로 브라우저의 중국어 줄바꿈 규칙을 따른다.

```css
word-break: normal;
overflow-wrap: break-word;
```

## 일본어

- 일본어는 문자 단위의 자연스러운 줄바꿈을 허용한다.
- 일본어의 문장 부호 및 줄바꿈 금칙 처리는 브라우저의 기본 언어 규칙을 우선한다.
- 한국어와 동일한 `word-break: keep-all`을 적용하지 않는다.
- 기본적으로 다음 규칙을 사용한다.

```css
word-break: normal;
overflow-wrap: break-word;
```

## 공통 규칙

- 모든 언어에서 문장형 텍스트에 `<br>`을 임의로 삽입하여 강제 줄바꿈하지 않는다.
- 화면 크기와 텍스트 컨테이너의 너비에 따라 자연스럽게 줄바꿈되어야 한다.
- 언어가 변경되어 문장 길이가 달라지더라도 레이아웃이 깨지지 않아야 한다.
- 텍스트 줄바꿈 문제를 해결하기 위해 문장 자체를 임의로 수정하거나 축약하지 않는다.
- 먼저 컨테이너의 width, max-width, padding, font-size, line-height 및 언어별 word-break 설정을 확인한다.
- 특정 언어에서만 발생하는 줄바꿈 문제는 해당 언어의 특성을 우선적으로 고려하여 해결한다.
- 기존 Figma 디자인의 레이아웃과 시각적 계층을 최대한 유지한다.