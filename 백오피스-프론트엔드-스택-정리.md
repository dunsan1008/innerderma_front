# 사내 백오피스 프론트엔드 & AI 코딩 운영 방식

> 스타트업 재무·경영 관리(번레이트·런웨이·매출·자산)를 다루는 사내 웹 대시보드를 만들며 실제로 쓰는 프론트엔드 기술 스택, 그리고 그 위에서 AI 코딩 도구(Claude Code)를 팀 규칙으로 길들여 쓰는 방법 정리. 도구 자랑이 아니라 "무엇을 왜 이렇게 쓰는지"가 목적.
>
> 학생 멘토링 참고용.

구성 한 줄 요약: **React 18 + TypeScript / Vite 빌드 / Zustand 상태 / Tailwind 스타일 / Cloudflare Pages 배포.** 서버는 Spring Boot(`/api/v1`), 인증은 Google OAuth 기반 JWT.

---

## 01. 프론트엔드 기술 스택

SPA(단일 페이지 앱) 한 벌. 아래는 실제 `package.json`에 잡혀 있는 의존성과 각자의 역할.

### 코어 · 언어 · 빌드

| 패키지 | 역할 — 왜 쓰는가 |
|---|---|
| **React** 18.3 | UI 라이브러리. 컴포넌트 단위로 화면을 조립한다. |
| **TypeScript** 5.6 | 정적 타입. 빌드가 곧 타입 검사(`tsc -b && vite build`)라 런타임 전에 실수를 잡는다. |
| **Vite** 6 | 개발 서버 + 번들러. 빠른 HMR, ESM 기반. `@ → src/` 경로 별칭. |
| **Yarn** 1.22 | 패키지 매니저(Classic). `yarn.lock`으로 버전 고정. |

### 상태 · 라우팅 · 통신

| 패키지 | 역할 — 왜 쓰는가 |
|---|---|
| **Zustand** 5 | 전역 상태. Redux보다 가벼운 훅 기반 스토어. 저장/편집 이중 버퍼(ST/DR)의 뼈대. |
| **react-router-dom** 7 | 클라이언트 라우팅. 사이드바 메뉴 트리와 URL을 잇는다. |
| **Axios** 1.13 | HTTP 클라이언트. 인터셉터가 토큰 자동 첨부 + 401 자동 갱신을 처리한다. |
| **@react-oauth/google** | Google OAuth 로그인. 사내 계정 게이트. |

### 화면 · 시각화

| 패키지 | 역할 — 왜 쓰는가 |
|---|---|
| **Tailwind CSS** 3.4 | 유틸리티 CSS. 색은 하드코딩 대신 CSS 변수(`--c-*`)로 다크/라이트 전환. |
| **Chart.js** + **react-chartjs-2** | 대시보드 차트(번레이트·런웨이 추이 등). |
| **react-datasheet-grid** | 엑셀 같은 편집 그리드. 회계 서식·붙여넣기 파싱 지원. |
| **exceljs** / **xlsx** | 엑셀 입출력. 서식 있는 생성은 exceljs, 파싱은 xlsx로 용도 분리. |
| **react-markdown** + **remark-gfm** | 사용 가이드 등 마크다운 렌더링. |
| **leaflet** | 지도 표시. |
| **@tabler/icons-react** | 아이콘 세트. 사이드바·헤더 아이콘. |

### 테스트 · 품질 · 배포

| 패키지 | 역할 — 왜 쓰는가 |
|---|---|
| **Vitest** 2 | 단위 테스트. 순수 헬퍼 위주 + `renderToStaticMarkup`으로 컴포넌트 HTML 검사. |
| **ESLint** / typescript-eslint | 정적 분석(현재 flat config 정비 필요 — 검증은 주로 `tsc`로). |
| **Cloudflare Pages** | 브랜치별 자동 빌드·배포. `staging`→스테이징 웹, `main`→운영 웹. |

> **멘토링 포인트.** 스택은 "유행하는 라이브러리 모음"이 아니라 **결정의 묶음**이다. 상태는 왜 Zustand인가(가벼움·훅), 차트는 왜 Chart.js인가(성숙도), 배포는 왜 Cloudflare Pages인가(정적 SPA에 무료 CDN). 각 칸의 "왜"를 설명할 수 있어야 스택을 안다고 할 수 있다.

---

## 02. 스택 위에 얹은 아키텍처 패턴

라이브러리보다 오래 남는 건 "우리 팀이 그걸 어떻게 쓰기로 약속했는가"다. 코드 리뷰에서 반복 지적되던 실수들을 규칙으로 굳혔다.

- **색상 단일 소스** — `theme.ts`의 `COLORS` 한 곳에서만 색을 정의한다. Tailwind는 CSS 변수로, Chart.js는 팔레트 함수로 참조. 그래서 다크/라이트 전환이 변수 재주입만으로 된다. `#fff` 직접 쓰기 금지.
- **공통 컴포넌트만** — `Button·Card·Table·PageHeader` 등 공통 컴포넌트로만 UI를 만든다. raw `<button>`을 새로 스타일링하지 않는다 — 디자인 일관성과 유지보수를 위해.
- **ST/DR 이중 버퍼** — 편집 중 값(DR·Draft)과 저장된 값(ST·Saved)을 분리. 대시보드 계산은 항상 ST 기준, 저장 눌러야 DR→ST 반영. 편집하다 만 숫자가 지표를 흔들지 않게.
- **bulk sync (N+1 금지)** — 목록 저장 시 행마다 API를 부르지 않는다. 바뀐 행만 모아 `PATCH /sync` 한 번. 20행 저장에 20번 호출하는 흔한 실수를 규칙으로 막았다.
- **API 도메인 클라이언트** — 컴포넌트에서 `axios`를 직접 부르지 않는다. `src/api/<도메인>.ts` 경유. 인증·토큰 갱신은 인터셉터가 전담해 화면 코드가 깨끗해진다.
- **가이드 시스템** — 경영팀용 사용법을 마크다운으로 두고, `screens` frontmatter로 "이 화면엔 이 도움말" 역인덱스를 만든다. 페이지에 자동으로 💡버튼이 붙는다.

---

## 03. AI 코딩 룰 — 규칙을 파일로 코드화

Claude Code(AI 코딩 에이전트)에게 매번 말로 설명하는 대신, 규칙을 저장소 안의 파일로 박아둔다. AI가 코드를 짜기 전에 이 규칙들을 읽고 따르게 만드는 게 핵심. 아래는 프론트엔드 `CLAUDE.md`의 "절대 규칙".

1. **색상값 하드코딩 금지** — `theme.ts` 단일 소스에서만. `#fff`·`text-[#...]` 직접 사용 금지.
2. **공통 컴포넌트로만 UI 제작** — raw `<button>/<table>/<select>`를 새로 스타일링하지 않는다.
3. **ST/DR 이중 버퍼 준수** — 편집=DR, 계산=ST. 계산을 DR로 하거나 편집을 ST에 직접 쓰지 않는다.
4. **API는 도메인 클라이언트로만** — 컴포넌트에서 `axios/fetch` 직접 호출 금지. 토큰은 인터셉터가 붙인다.
5. **백엔드 계약은 camelCase** — 응답 `id`는 항상 보존해 CRUD 동기화가 어긋나지 않게.
6. **차트 인스턴스 cleanup** — Chart.js는 재생성 전 정리 — 메모리 누수·유령 캔버스 방지.
7. **목록 저장은 N+1 금지** — 행마다 호출하지 말고 변경분만 모아 bulk `sync` 1회.
8. **사이드바 아이콘은 두 곳 등록** — 데이터(`navigation.ts`)와 렌더 매핑(`navIcons.ts`) 둘 다 — 실제 사고 경험이 규칙이 된 사례.

> **왜 파일에 규칙을 쓰나?** 사람에게 "이건 이렇게 해줘"라고 매번 말하는 대신 저장소에 적어두면, AI든 새 팀원이든 **같은 문서를 읽고 같은 방식으로 일한다**. 규칙 8번처럼 실제 버그가 한 번 나면 그 교훈을 문서로 남겨 재발을 막는다 — 문서가 곧 팀의 기억이다.

---

## 04. 시스템 전역 불변식 (프론트 너머까지)

프론트만이 아니라 백엔드·데이터·인프라를 아우르는 "어디서든 지키는 경계". 스택을 넘어서는 설계 규율의 예시.

- **DB 외래키(FK) 제약 금지** — 관계는 ID 컬럼 + 인덱스로만. 서비스 분리·마이그레이션 유연성을 위해.
- **설정·시크릿은 Vault로** — 코드/yaml 하드코딩 금지. 부팅 시 로드, 변경 시 롤아웃.
- **AI/지식 처리는 전용 서비스(Python)가 직접** — 백엔드(Kotlin)는 프록시로 위임만. 책임 경계를 흐리지 않는다.
- **API 네임스페이스 분리** — 내부용·외부 노출용·경영데이터용 경로를 나눠 혼선을 막는다.
- **DB 스키마는 마이그레이션(Flyway)으로만** — 운영은 `ddl-auto=validate`. 손으로 스키마를 바꾸지 않는다.

---

## 05. 하네스 — AI를 팀에 태우는 골격

"하네스(harness)"란 AI 코딩 도구가 저장소 규칙·워크플로 안에서 안전하게 굴러가도록 감싸는 틀이다. 규칙 파일 계층, 필요할 때만 읽는 상세 규칙, 재현 가능한 작업 절차(런북), 그리고 격리된 작업 공간으로 이뤄진다.

**Layer 1 — 계층형 규칙 로딩**
전역 규칙(개인 설정) → 저장소 루트 `CLAUDE.md` → 하위 `frontend/CLAUDE.md` 순으로 좁혀진다. 항상 켜져 있는 "절대 규칙"은 여기에.

⬇

**Layer 2 — 온디맨드 상세 규칙**
`.claude/rules/*.md`(architecture·layout·build-test·deploy)는 자동 로드되지 않는다. 해당 작업을 할 때만 읽어 컨텍스트(토큰)를 아낀다 — 상시 규칙은 짧게, 깊은 규칙은 필요할 때만.

⬇

**Layer 3 — 런북 스킬**
`.claude/skills/`에 배포(release)·긴급패치(hotfix) 같은 절차를 단계별 문서로 둔다. AI가 "릴리스해줘"를 받으면 이 런북을 순서대로 실행한다 — 사람이 하던 체크리스트의 코드화.

⬇

**Layer 4 — 워크트리 격리 + PR 게이트**
새 작업은 `.worktrees/<branch>`에 격리된 별도 작업 공간을 파서 진행한다. `main`·`staging`엔 직접 커밋하지 않고 항상 **브랜치 → PR** 경유. 실배포 머지는 사람이 승인한다.

⬇

**Layer 5 — 검증 문화**
"완료"라고 말하기 전에 실제로 돌려 확인한다. 스테이징에 배포한 뒤 개발용 토큰으로 실제 화면을 클릭해 검증 — 테스트 통과만으로 끝내지 않는다.

> **핵심 발상.** AI에게 자유를 다 주는 게 아니라, **사람이 지키던 규칙·절차·게이트를 문서로 만들어 그 안에서 일하게** 한다. 규칙은 짧게 상시 로드, 깊은 지식은 필요할 때만, 위험한 작업엔 사람 승인 게이트 — 이 조합이 하네스다.

```bash
# 작업 격리의 실제 모습 — 브랜치별 워크트리를 파서 그 안에서만 작업
git worktree add .worktrees/feat/payroll -b feat/payroll --no-track origin/staging

# 규칙 로딩 계층 (좁아질수록 구체적)
~/.claude/CLAUDE.md        # 전역: git·워크트리·문서 스타일 규칙
CLAUDE.md                  # 루트: 시스템 전역 불변식·배포 플로우
frontend/CLAUDE.md         # 프론트: 절대 규칙 8개 (항상 로드)
frontend/.claude/rules/    # 상세: 필요할 때만 읽음
frontend/.claude/skills/   # 런북: release / hotfix 절차
```

---

*실무 백오피스 프로젝트에서 실제 사용 중인 구성 정리 · 학생 멘토링 참고용*
*React 18 · TS · Vite · Zustand · Tailwind · Chart.js · Claude Code*

---

# 부록. InnerDerma 프론트엔드 스택 (AAC AI 피부 사후관리 웹앱)

> 위 본문은 사내 백오피스 대시보드(React + **TypeScript**) 기준이다.
> InnerDerma 는 별개 프로젝트이며 **TypeScript 를 쓰지 않고 JavaScript 로만** 구성했다.
> 두 프로젝트의 차이를 명확히 하기 위해 덮어쓰지 않고 부록으로 분리해 기록한다.

경로: `frontend/` · 구성 한 줄 요약: **React 18 + JavaScript / Vite 6 빌드 / Zustand 상태 / Tailwind 스타일.**
Figma `InnerDerma > mcp_frontend` 페이지의 20개 프레임을 아이폰 16(393x852) 기준으로 퍼블리싱했고, 서버는 추후 `/api/v1` 로 연동한다.

## A. 기술 스택

### 코어 · 언어 · 빌드

| 패키지 | 역할 — 왜 쓰는가 |
|---|---|
| **React** 18.3 | UI 라이브러리. 화면 1개 = Figma 프레임 1개로 맞춰 컴포넌트를 나눴다. |
| **JavaScript (ESM)** | 타입 검사 없이 `.js` / `.jsx` 만 사용. 빌드는 `vite build` 단독. **`tsc` 단계가 없다.** |
| **Vite** 6.0 | 개발 서버 + 번들러. `@ → src/` 경로 별칭. |
| **npm** | 패키지 매니저. 모든 의존성 **버전 고정**(캐럿 없음)으로 재현성 확보. |

> **백오피스와의 차이.** 백오피스는 `tsc -b && vite build` 라 빌드가 곧 타입 검사지만, 여기는 타입 검사 단계가 없다.
> 그 대신 **Figma 원본과의 픽셀 대조**를 검증 게이트로 삼았다(아래 D 항목).

### 상태 · 라우팅 · 통신

| 패키지 | 역할 |
|---|---|
| **Zustand** 5 | 전역 상태. 도메인별로 스토어를 쪼갰다 — `careStore`(케어 사이클·선택 날짜·촬영 여부·**날짜별 루틴 수행 기록**), `wishlistStore`(찜, localStorage 영속), `uiStore`(오버레이 열림), `onboardingStore`(사용자 유형). `careStore`·`wishlistStore` 는 `persist` 로 localStorage 에 남는다. |
| **react-router-dom** 7 | 화면 전환. 단, **오버레이는 라우트로 만들지 않는다**(아래 C 항목). |
| **Axios** 1.7 | HTTP 클라이언트. 인터셉터가 토큰 첨부와 401 처리를 맡는다. |

### 화면 · 스타일

| 패키지 | 역할 |
|---|---|
| **Tailwind CSS** 3.4 | 유틸리티 CSS. 색·폰트는 `src/theme.js` 단일 소스를 `tailwind.config.js` 가 읽어 생성한다. |
| Google Fonts | 본문 **Noto Sans KR**, 큰 카피 **42dot Sans**. 로고는 Palatino 계열. |

### 검증 (개발 전용)

| 패키지 | 역할 |
|---|---|
| **Playwright** 1.49 + Chromium | 배포물에 포함되지 않는 devDependency. Figma 원본과 픽셀 대조하고 기능 동작을 자동 검증한다. |

---

## B. 프로젝트 구조

```
frontend/src
├─ theme.js              # 색·폰트·프레임 규격 단일 소스 (tailwind.config.js 가 import)
├─ lib/useMountTransition.js  # 모달 여닫힘 애니메이션용 마운트 지연 훅
├─ lib/calendar.js       # (위 참고) 실제 날짜 기준 달력 계산
├─ hooks/useCamera.js    # getUserMedia 스트림 + 프레임 캡처
├─ api/                  # client.js(axios) · care.js · market.js  ← 도메인별 모듈
├─ store/                # careStore · wishlistStore · cartStore · uiStore · onboardingStore
├─ constants/            # Figma 문구·좌표 더미 데이터 (routines, marketProducts, marketScreens,
│                        #  selfCheck, cartItems, productDetail)
├─ components/
│  ├─ layout/            # DeviceFrame · Screen · StatusBar · TabBar(전 화면 공용) · AppModals
│  ├─ home/ routine/ market/ filter/ ui/
└─ pages/                # 라우트 단위 화면
```

### 라우트

| 경로 | 화면 | Figma |
|---|---|---|
| `/` → `/signup` → `/connecting` → `/connected` | 첫 화면 · 가입 · 방문기록 연결 · 연결 완료 | 온보딩 |
| `/home` | 촬영 여부로 분기 (`HomeRoute`) | 870:3572 / 870:3771 |
| `/home/first-visit` | 최초 접속 홈 (검증용 직접 진입) | 870:3572 |
| `/camera` → `/solution-summary` → `/solution/:cycle` | 촬영 · 한 줄 정리 · 나이트/모닝 루틴 | 870:3650 / 870:3761 / 870:3771·870:4002 |
| `/self-check` → `/solution-loading` | 데일리 자가 진단 · 솔루션 생성 로딩 | 970:1090 / 970:1129 |
| `/mypage` | 마이페이지 | 마이페이지 |
| `/market` · `/market/oily` · `/market/elasticity` | 피쓰 서울 마켓 (전체 / 수부지 / 피부탄력) | 마켓 1·2·3 |
| `/market/wim` | 윔 스토어 (좌표는 마켓 1 과 동일) | 마켓 4 (1117:1689) |
| `/market/wishlist` | MY 찜 (개별 선택 · 선택삭제) | 870:5089 |
| `/market/cart` | MY 장바구니 | 1026:2397 |
| `/market/product/:id` | 상품 상세 | 1026:2575 |
| `/market/filter/{gender,age,diagnosis}` | 필터 바텀시트 | 필터 |

진입 경로: 마켓 헤더의 가방 아이콘 → `/market/cart`, 상품 카드 본문 → `/market/product/:id`.
`/self-check` 는 아직 화면 안에 진입 버튼이 없다(Figma 에 해당 버튼이 그려진 프레임이 없어 촬영 플로우에 끼워 넣지 않았다). 촬영 뒤에 넣을지 확인이 필요하다.

---

## C. 이 프로젝트에서 굳힌 규칙

- **색상 단일 소스** — `theme.js` 의 `COLORS` 에만 색을 정의한다. 컴포넌트에서 `#fff`·`text-[#...]` 직접 사용 금지. Figma 에서 뽑은 값은 전부 여기에 이름을 붙여 등록했다.
- **API 는 도메인 모듈로만** — 화면에서 `axios` 를 직접 부르지 않고 `src/api/<도메인>.js` 를 경유한다. 토큰은 인터셉터가 붙인다.
- **오버레이는 라우트가 아니다** — 전체 캘린더와 세안 확인은 화면을 갈아타는 게 아니라 위에 겹치는 것이므로 `uiStore` 상태로 열고 닫고, `AppModals` 가 한 곳에서 렌더한다. URL 이 바뀌지 않아 뒤로가기 동작이 자연스럽다.
- **앱 셸은 스케일로 맞춘다** — 프레임 크기(393x852)를 줄이면 내부 좌표가 전부 어긋난다. `DeviceFrame` 은 크기를 유지하고 `transform: scale` 로만 축소해 브라우저 창이 작아도 페이지가 스크롤되지 않게 한다.
- **고정 레이어와 스크롤 영역 분리** — `Screen` 이 `header` / `tabBar` 를 스크롤 밖 고정 레이어로 받고, 가운데만 스크롤한다. 콘텐츠 좌표는 Figma 프레임 기준을 그대로 쓰고 `Screen` 이 `headerHeight` 만큼 끌어올려 보정한다.
- **디자인 좌표는 절대 배치, 다만 프레임 높이를 신뢰** — Figma 에 프레임 높이가 내용 합계보다 큰 블록이 있어(예: 모닝 StepList 435 vs 내용 406) flow 로 쌓으면 오차가 누적된다. 블록마다 Figma y 좌표를 상수로 두고 절대 배치했다.
- **하단 네비게이션은 한 컴포넌트만 쓴다** — Figma 에는 루틴 화면용 탭바(870:3984)와 마켓용 탭바(870:6024)가 따로 그려져 있었지만, 화면마다 모양이 달라 보이는 문제가 있어 마켓 탭바(`components/layout/TabBar.jsx`) 하나로 통일했다. 전 화면에서 프레임 좌표 (0, 756) 96px 높이로 동일하다.
- **모달은 밖을 누르면 닫힌다** — 캘린더·세안 확인·필터 시트 모두 딤 영역이 닫기 버튼이다. 여닫힘은 `useMountTransition` 으로 닫힘 트랜지션이 끝날 때까지 마운트를 유지해 애니메이션이 잘리지 않게 했다(`requestAnimationFrame` 2중 호출로 열림 시작 프레임도 보존).
- **캘린더는 실제 날짜로 돈다** — 처음엔 Figma 프레임의 2026년 8월을 상수로 박고 "며칠"이라는 숫자로 계산했는데, 월·연도 이동을 붙이려면 달을 넘나들어야 해서 `lib/calendar.js` 를 **날짜키('YYYY-MM-DD') 단위**로 다시 짰다. 오늘은 `new Date()` 에서 계산하고, 더미 수행 기록도 오늘 기준 상대 오프셋(-7·-6·-5·-3·-1일)으로 만든다.
- **칩은 상태, 선택은 테두리** — 초록=수행 완료 / 흰색=오늘 / 회색=지난 날 미수행 / 미래=칩 없음. 여기에 "지금 보고 있는 날"을 색으로 겹치면 규칙이 무너지므로 선택은 앰버(`chip-select`) 테두리로만 표시한다.
- **미래 날짜에는 솔루션이 없다** — 솔루션은 그날 촬영·분석 결과물이라 아직 오지 않은 날에는 만들어질 수 없다. `RoutineScreen` 이 `isFutureDate` 로 판단해 본문을 `NoSolutionNotice` 로 바꾸고 수행 완료 버튼도 내린다.
- **사이클 전환은 한 라우트에서** — night/morning 을 라우트 두 개로 두면 오갈 때마다 화면이 리마운트돼서 세그먼트 선택 표시가 미끄러지지 않고 툭 튄다. `/solution/:cycle` 한 라우트로 묶어 컴포넌트를 유지하고, 선택 표시는 `translateX` 로 옮기고 본문만 `key={cycle}` 로 페이드 인 한다.
- **캘린더 초록 표시는 상태에서 나온다** — 초록 칩은 "그날 루틴을 수행 완료했다"는 뜻이고 근거는 `careStore.completedDates` 다. 정적 더미가 아니라 수행 완료 버튼을 누르면 즉시 캘린더와 주간 스트립에 반영된다. 수행 완료는 **당일에만** 기록할 수 있다.
- **다른 기기 테스트는 HTTPS 로 띄운다** — 브라우저의 `getUserMedia`(카메라)는 **보안 컨텍스트**에서만 노출된다.
  `localhost` 는 예외로 허용되지만 다른 기기에서 `http://LAN-IP` 로 접속하면 OS 와 무관하게
  `navigator.mediaDevices` 자체가 `undefined` 가 되어 카메라를 쓸 수 없다(윈도우 문제가 아니다).
  그래서 `HTTPS=1` 로 자체 서명 인증서를 켜는 모드를 준비했다: `npm run dev:https` / `npm run preview:https`.
  기본 인증서에는 `127.0.0.1` 만 들어가 LAN IP 접속 시 SSL 핸드셰이크가 실패하므로,
  `vite.config.js` 가 `os.networkInterfaces()` 로 이 PC 의 LAN IP 를 찾아 인증서 SAN 에 넣는다.
  자체 서명이라 접속 시 브라우저 경고가 뜨는데 "고급 → 계속" 으로 진행하면 카메라가 동작한다.
  포트를 이미 쓰는 HTTP 서버가 떠 있으면 그쪽이 IPv4 를 점유해 SSL 에러가 나므로 먼저 종료해야 한다.
- **Figma 출력은 그대로 믿지 않는다** — 숨겨진 레이어(노치), 자식 없는 빈 프레임(`boxicons:heart`), 같은 컴포넌트의 다른 인스턴스(홈 인디케이터 알약) 때문에 코드 출력과 실제 렌더가 다르다. 프레임 스크린샷·픽셀 실측으로 확인한 뒤 반영했다.
  - **겹쳐 있는 레이어는 위에 있는 것만 보인다** — 자가진단(970:1090)은 같은 좌표에 버튼이 두 개씩 놓여 있다. `970:1111`/`970:1113` 은 같은 문구 중복이라 한 번만 그리고, `970:1115`("그 외")/`970:1117`("아무 이상이 없어요.") 는 뒤에 있는 `1117` 이 위로 그려지므로 그 문구만 항목으로 쓴다("그 외"는 아래 카드 `970:1119` 로 따로 존재).
  - **인스턴스 내부가 전부 숨김이면 빈 블록이다** — ProductDetail(1026:2575)의 상세 설명 3블록은 루틴 카드 인스턴스지만 내부 그룹이 모두 `hidden` 이라, 실제로는 `rgba(177,177,177,0.46)` 배경 + 2px `#f3f3f3` 테두리 빈 자리표시로 보인다. 카드 내용을 상상해 채우지 않고 자리표시 그대로 옮겼다.
- **넓은 텍스트 박스가 버튼 클릭을 막는다** — Figma 는 텍스트 박스가 이웃 요소 위로 넘쳐도 상관없지만(정적 렌더) 브라우저에서는 그 박스가 클릭을 가로챈다. ProductDetail 상품명(내부 텍스트 338px > 바깥 박스 214px)이 찜·공유 버튼을 덮어 눌리지 않았다. 텍스트에는 `pointer-events-none`, 버튼에는 `z-10` 을 줘서 좌표는 그대로 두고 히트 영역만 살렸다.
- **카드 전체 클릭은 오버레이 버튼으로** — `PostCard` 는 상세 이동용 투명 버튼을 카드 맨 뒤(`z-0`)에 깔고, 하트(`z-10`)와 선택 체크박스(`z-20`)를 그 위에 올린다. 카드 바깥 `div` 에 `onClick` 을 걸면 내부 버튼과 클릭이 엉킨다.
- **상품명은 18자로 자른다** — Figma 는 제품마다 이름을 2~3줄로 손수 쪼개 뒀는데, 길이가 제각각이라 카드 높이와 태그 줄이 밀렸다. `lib/productName.js` 의 `truncateProductName` 으로 **공백 포함 18자**까지만 남기고 초과분은 `…` 한 글자로 대체한다(마켓 카드·추천 배너·상품 상세 공통). 줄바꿈은 브라우저에 맡긴다.
- **토글은 pill 하나를 움직인다** — 좌우 버튼의 배경색을 각각 칠했다 지우면 전환이 툭툭 끊긴다. `StoreToggle` 은 검정 pill 을 DOM 에 하나만 두고 `translateX` 로 미끄러뜨리고, 로고는 고해상도 원본 1장을 `filter: invert` 로 반전시켜 색을 바꾼다. 흑백 상태는 `wim` **boolean** 하나로 결정된다.
- **저해상도 변형 PNG 를 만들지 않는다** — 같은 로고의 흑/백 버전을 각각 내보내면 크기가 다른 파일(88x32 vs 2500x918)이 섞여 한쪽이 흐려진다. 고해상도 1장 + CSS 필터로 통일했다.
- **두 스토어는 한 컴포넌트로** — 피쓰 서울(마켓 1·2·3)과 윔 스토어(마켓 4)는 `MarketScreen` 의 `variant` 로만 갈린다. `MARKET_SCREENS.wim` 이 마켓 1 과 **똑같은 좌표·카드 규격**을 쓰고 상품 데이터만 다르다. 그래서 토글 위치가 항상 y=536 으로 같고, 전환해도 배너·탭·카드가 제자리에서 내용만 바뀐다.
- **상세는 목록과 같은 상품 객체를 본다** — 상세 페이지가 자기 더미를 들고 있으면 목록에서 누른 찜이 반영되지 않는다. `findProductByKey(라우트 파라미터)` 로 목록과 **같은 객체**를 찾아 대표 이미지·이름·가격·태그를 모두 거기서 가져오고, 찜도 `wishlistStore` 를 직접 쓴다.

---

## D. 검증 방식 — 픽셀 대조와 기능 자동 검증

타입 검사가 없는 만큼 검증을 두 축으로 세웠다. 모두 `frontend/scripts/` 의 개발 전용 스크립트다.

| 스크립트 | 역할 |
|---|---|
| `shoot.mjs` | 구현 화면을 아이폰 프레임 단위로 촬영 (852 초과 화면은 전체 길이도) |
| `diff.mjs` | Figma 원본과 픽셀 비교. 좌(Figma)/중(구현)/우(차이) 3분할 이미지 + 불일치율 + 문제 y구간 출력 |
| `zoom.mjs` | 특정 y 구간 확대 비교 (폰트 굵기·자간 확인) |
| `px.mjs` | 특정 좌표 픽셀값 조사 (숨은 레이어·딤 투명도 판별) |
| `verify.mjs` | 기능 동작 자동 검증 (스크롤 고정, 모달 여닫힘·외부 클릭, 카메라, 찜 토글·개별 선택·상세 동기화, 수행 완료 기록, 탭바 통일, 월·연 이동, 미래 날짜 안내, 세그먼트 전환, 자가진단·장바구니·상품 상세, 상품명 18자 제한, 스토어 토글, 베스트 조합 접기/펴기 등 **129개 항목**). 앱이 실제 날짜로 도는 만큼 기대값도 스크립트가 실행 시점 날짜에서 계산한다(날짜 하드코딩 금지). |
| `shoot-new.mjs` | 신규 화면(자가진단·솔루션 생성 로딩·장바구니·상품 상세)을 프레임 1:1(393px)로 촬영. `DeviceFrame` 의 `scale` 을 풀고 실제 프레임 높이로 펼쳐 Figma 원본과 바로 겹쳐 볼 수 있게 한다. |
| `verify-i18n.mjs` | 다국어 검증. 4개 언어 × 10개 라우트를 돌며 ①`{t.}` 리터럴 노출 ②실제 번역 적용(기대 문구 존재 + 한국어 잔존) ③새로고침 후 언어 유지 를 확인한다. **"리터럴이 없다"만 보면 미번역을 놓치므로 반드시 기대 문구까지 검사한다.** |

`diff.mjs` 는 Figma 내보내기의 그림자 여백을 알파 채널로 자동 감지해 프레임 원점을 잡고, 프레임 밖(라운드 코너·딤)은 비교 대상에서 제외한다.

**검증 스크립트 실행 조건 두 가지** — 둘 다 빠지면 전부 실패로 보인다.
- `locale: 'ko-KR'` — 다국어를 붙인 뒤로 앱이 `navigator.languages` 로 언어를 추론한다. Playwright 기본 로케일은 `en-US` 라서 `aria-label`("촬영", "전체 캘린더" 등)이 영어로 바뀌어 한국어로 요소를 찾는 검증이 통째로 깨진다.
- `ignoreHTTPSErrors: true` — `npm run preview:https` 로 띄운 서버는 자체 서명 인증서를 쓴다. 실행 인자로 `https://localhost:4173` 을 넘긴다.

**대조 결과** — 20개 프레임 불일치율 0.78% ~ 6.17%. 남은 차이는 전부 텍스트 글리프 래스터화(Figma 텍스트 엔진 vs 브라우저) 이며 레이아웃·색·에셋은 일치한다.

나중에 추가한 4개 프레임(자가진단 970:1090 · 로딩중-솔루션 생성 970:1129 · MY 장바구니 1026:2397 · ProductDetail 1026:2575)은 `diff.mjs` 수치 대조 대신 `shoot-new.mjs` 스크린샷과 Figma 렌더를 나란히 놓고 확인했다. 좌표·색·타이포는 `get_design_context` 실측값을 그대로 상수로 박아 두었다.

**정적 디자인과 동작이 어긋나는 곳은 동작을 택했다** (Figma 는 스냅샷 하나뿐이라 상태를 표현하지 못한다)
- 자가진단: Figma 스냅샷은 항목 하나가 선택된 채 저장 버튼이 검정이지만, 실제로는 아무것도 안 고르면 저장할 수 없어야 하므로 비활성(회색)으로 시작한다.
- 장바구니 전체 체크박스: Figma 는 체크 하나뿐이지만 3개 중 2개만 선택된 상태를 구분해야 해서 부분선택은 하이픈 + `aria-checked="mixed"` 로 표시한다.
- 장바구니 배송방법: Figma 는 정적 텍스트지만 실제로 고를 수 있어야 하므로 `<select>` 를 `opacity-0` 으로 겹쳐 뒀다.

---

## E. 백엔드 연동 준비 상태

- 화면 문구·좌표는 `src/constants/*.js` 의 더미 데이터로 동작한다. 교체 지점을 주석으로 표시해 두었다.
- `api/care.js` — 방문 기록 연결, 사진 업로드(하루 한 번 제한은 서버가 현지 날짜로 판단), 자가 상태, 케어 사이클 조회, 월별 기록, 루틴 완료.
- `api/market.js` — 추천 상품, 찜 조회·토글·선택삭제.
- 신규 화면의 교체 지점
  - `constants/selfCheck.js` → `GET /api/v1/self-check/items`, 저장은 `POST /api/v1/self-check`
  - `constants/cartItems.js` · `store/cartStore.js` → `/api/v1/cart` CRUD
  - `constants/productDetail.js` → `GET /api/v1/products/{id}`
  - `SolutionLoadingScreen` 의 `setTimeout(1800)` → 솔루션 생성 API 응답 대기
- 응답 필드는 기획서 15장 데이터 모델(`SkinCapture` · `SkinAnalysis` · `SelfCheck` · `CareCycle` · `ProductUsage`)을 따라 camelCase 로 잡았다.

*InnerDerma 프론트엔드 · React 18 · JavaScript · Vite · Zustand · Tailwind · Playwright(검증)*
