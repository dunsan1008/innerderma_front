# Claude 작업 방식 — 프로젝트 선호사항

## 0. 배포 작업

이 레포의 배포 관련 작업은 `DEPLOY_GUIDE.md`(이 레포가 프론트엔드로서 맡는 부분)와 `INNERDERMA_DEPLOY_GUIDE.md`(InnerDerma 백엔드 레포 기준 전체 인프라)를 참고한다. 이 레포는 자체 컨테이너가 없는 정적 빌드 산출물이므로, 백엔드/인프라 쪽 변경(nginx, 인증서, 다른 서비스 컨테이너)이 필요해 보이면 실행 전 사용자에게 먼저 확인한다.

## 1. 커밋 / 푸시 규칙

작업(기능 구현, 버그 수정 등)이 **검증된 뒤에만** — 테스트 통과, 빌드 성공, 또는 기능이 실제로 동작하는 것을 확인한 뒤 — `git commit`과 `git push`를 진행한다. 단, 실행 전에 반드시 **사용자에게 확인**을 받는다. 자동으로 커밋·푸시까지 이어가지 않는다. (기본값은 "확인 후 진행"이며, 자동 진행은 사용자가 해당 세션에서 별도로 명시할 때만 예외적으로 허용한다.)

## 2. 응답 언어

모든 응답은 **한글**로 작성한다. 사용자가 명시적으로 다른 언어를 요청하지 않는 한 영어 등 다른 언어로 응답하지 않는다.

## 3. 간단/반복 작업은 Antigravity와 분담

간단하거나 정형화된(routine) 작업은 Claude 혼자 다 처리하기보다, **Antigravity 에이전트와 나눠서** 진행하는 것을 선호한다. oh-my-claudecode의 `/ask antigravity` 경로(Claude-Codex-Gemini-Antigravity 통합 오케스트레이션의 일부)를 통해 기계적이고 단순한 작업을 Antigravity로 라우팅하고, 복잡하거나 판단이 필요한 작업은 Claude가 직접 처리한다. 작업마다 "이건 Antigravity에 넘길 만큼 단순한가"를 판단해서 결정한다. (협업 대상은 Gemini가 아니라 Antigravity.)

## 4. Figma 작업 — 스코프는 사용자에게 물어보기

Figma 파일이 크면 `get_metadata`로 페이지/문서 전체를 훑어서 화면 목록을 스스로 알아내려는 시도는 지양한다. 이렇게 했다가 수십만 자 단위의 덤프가 나와 디스크에 페이징하고 grep/jq로 파싱하느라 여러 번 삽질한 적이 있다.

**대신**: 사용자가 특정 node-id 링크를 줬으면 그 범위만 정확히 구현한다. 범위가 불명확하면(링크가 없거나 여러 화면이 걸려있으면) 전체 프레임 트리를 스스로 탐색하지 말고 **사용자에게 직접 어떤 화면을 작업할지 물어본다**. `get_metadata`는 이미 특정된 노드를 드릴다운하는 등 작고 타겟팅된 용도로만 쓰고, "사용자에게 묻기"의 대체 수단으로 쓰지 않는다.

## 5. Figma 구현 — 스크린샷만 보지 말고 전체 컨텍스트를 읽기

Figma 화면을 스크린샷 기반으로만 재구성하면 상태 뱃지 색상, 카드 배경, 라벨 텍스트 등 요소별 세부사항이 실제 디자인과 미묘하게 어긋나는 경우가 있다. 스크린샷만으로는 안 보이는 것들: 항목별로 고정된 색상 vs UI 선택 상태를 나타내는 색상의 구분, 비슷해 보이지만 실제로 다른 라벨 텍스트, 카드가 다크 테마인지 화이트 테마인지 같은 구조적 선택.

**교훈**: 스크린샷은 레이아웃을 빠르게 훑어보거나 중복/구버전 프레임을 가려낼 때는 유용하지만, 화면의 세밀한 시각적 디테일(항목별 색상 코딩, 뱃지 텍스트, 카드 배경, 테두리 의미)까지 정확히 재현하려면 신뢰할 수 없다. 상태를 많이 가진 화면을 "구현"할 때는 단순히 방향만 잡는 게 아니라, `get_design_context`가 반환하는 실제 JSX/Tailwind 전체를 읽고 필요하면 청크로 나눠서라도 끝까지 확인한 뒤 컴포넌트를 작성한다.
